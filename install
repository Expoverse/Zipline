version='0.0.1'
platform='linux'
curl -Lo zipline.tar.gz https://github.com/Expoverse/Zipline/releases/download/$version/zipline-linux-amd64.tar.gz
tar zxf zipline.tar.gz && sudo mv zipline /usr/local/bin/zipline && rm zipline.tar.gz
mkdir -p ~/.zipline